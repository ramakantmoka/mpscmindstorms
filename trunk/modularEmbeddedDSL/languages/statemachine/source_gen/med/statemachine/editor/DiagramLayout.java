package med.statemachine.editor;

/*Generated by MPS */

import jetbrains.mps.nodeEditor.cellLayout.AbstractCellLayout;
import jetbrains.mps.nodeEditor.cells.EditorCell_Collection;
import jetbrains.mps.nodeEditor.cells.EditorCell;
import jetbrains.mps.nodeEditor.text.TextBuilder;

public class DiagramLayout extends AbstractCellLayout {
  public DiagramLayout() {
  }

  public void doLayout(EditorCell_Collection collection) {
    int ox = collection.getX() + 200;
    int oy = collection.getY() + 200;
    collection.setWidth(500);
    collection.setHeight(500);
    int noOfChilren = collection.getChildCount();
    double da = 2 * Math.PI / noOfChilren;
    double angle = 0;
    for (EditorCell cell : collection) {
      cell.setX((int) (ox + 100 * Math.sin(angle)));
      cell.setY((int) (ox + 100 * Math.cos(angle)));
      angle += da;
    }
  }

  public TextBuilder doLayoutText(Iterable<EditorCell> iterable) {
    return null;
  }
}
