package med.requirements.rt;

/*Generated by MPS */

import javax.swing.JComponent;
import jetbrains.mps.nodeEditor.EditorContext;
import jetbrains.mps.smodel.SNode;
import javax.swing.JPanel;
import java.awt.Dimension;
import java.awt.BorderLayout;
import javax.swing.JTable;
import javax.swing.JScrollPane;
import java.awt.Color;

public class ReqEditorUtil {
  public ReqEditorUtil() {
  }

  public static JComponent create(EditorContext ctx, SNode req) {
    JPanel root = new JPanel();
    /*
      root.setPreferredSize(ctx.getNodeEditorComponent().getSize());
    */
    root.setPreferredSize(new Dimension(1000, 600));
    root.setLayout(new BorderLayout());
    /*
      JPanel buttonPanel = new JPanel();
    */
    /*
      root.add(buttonPanel, BorderLayout.NORTH);
    */
    RequirementsTableModel m = new RequirementsTableModel(req);
    JTable table = new JTable(m);
    JScrollPane pane = new JScrollPane(table);
    pane.setBackground(new Color(255, 255, 255));
    root.add(pane, BorderLayout.CENTER);
    return root;
  }
}
