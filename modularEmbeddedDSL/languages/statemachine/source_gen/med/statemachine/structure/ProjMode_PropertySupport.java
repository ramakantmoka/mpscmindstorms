package med.statemachine.structure;

/*Generated by MPS */

import jetbrains.mps.smodel.PropertySupport;
import java.util.Iterator;
import jetbrains.mps.internal.collections.runtime.ListSequence;

public class ProjMode_PropertySupport extends PropertySupport {
  public boolean canSetValue(String value) {
    if (value == null) {
      return true;
    }
    Iterator<ProjMode> constants = ListSequence.fromList(ProjMode.getConstants()).iterator();
    while (constants.hasNext()) {
      ProjMode constant = constants.next();
      if (value.equals(constant.getName())) {
        return true;
      }
    }
    return false;
  }

  public String toInternalValue(String value) {
    if (value == null) {
      return null;
    }
    Iterator<ProjMode> constants = ListSequence.fromList(ProjMode.getConstants()).iterator();
    while (constants.hasNext()) {
      ProjMode constant = constants.next();
      if (value.equals(constant.getName())) {
        return constant.getValueAsString();
      }
    }
    return null;
  }

  public String fromInternalValue(String value) {
    ProjMode constant = ProjMode.parseValue(value);
    if (constant != null) {
      return constant.getName();
    }
    return "";
  }
}
